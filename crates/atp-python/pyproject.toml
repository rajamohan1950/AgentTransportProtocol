[build-system]
requires = ["maturin>=1.5"]
build-backend = "maturin"

[project]
name = "atp"
version = "0.1.0"
description = "Agent Transport Protocol - Python SDK"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "atp"
